{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "window-effects",
  "description": "Allow toggling the main window vibrancy effect at runtime.",
  "windows": ["main"],
  "permissions": ["core:window:allow-set-effects"]
}
