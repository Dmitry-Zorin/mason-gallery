/**
 * Height (px) of the custom/native title bar. Shared by the shell's content
 * top-offset, the macOS drag region, and the settings drawer's top inset so
 * the three stay aligned.
 */
export const TITLEBAR_HEIGHT = 36;
